{
  "name": "{{PROJECT_NAME}}",
  "version": "1.0.0",
  "scripts": {
    "prepare": "hive prepare ./hive",
    "dev": "hive prepare ./hive && HIVE_SRC=$PWD/hive npm -C .hive run dev",
    "start": "hive prepare ./hive && HIVE_SRC=$PWD/hive npm -C .hive run start"
  },
  "dependencies": {
    "@koa/multer": "^4.0.0",
    "@paralect/hive": "{{HIVE_DEP}}",
    "lodash": "^4.17.23",
    "moment": "^2.30.1"
  }
}